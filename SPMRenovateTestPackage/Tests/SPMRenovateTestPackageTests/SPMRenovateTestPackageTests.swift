import XCTest
@testable import SPMRenovateTestPackage

final class SPMRenovateTestPackageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SPMRenovateTestPackage().text, "Hello, World!")
    }
}
